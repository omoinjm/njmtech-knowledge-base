{
  "name": "blob-cron",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/blob-cron/src",
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "poetry run python main.py",
        "cwd": "apps/blob-cron",
        "envFile": "environments/blob-cron.env"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pytest",
        "cwd": "apps/blob-cron",
        "envFile": "environments/blob-cron.env"
      }
    }
  }
}
