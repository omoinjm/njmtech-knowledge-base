[project]
name = "njmtech-upload-blob"
version = "0.1.1"
description = "Cloudflare Worker API for uploading files to Vercel Blob storage."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
